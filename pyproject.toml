[build-system]
requires = ["setuptools>=68", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "kaggle-llamacpp-backend"
version = "0.3.0"
requires-python = ">=3.10"
dependencies = ["requests>=2.31.0", "tqdm>=4.66.0"]

[tool.setuptools.packages.find]
where = ["src"]
